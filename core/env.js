// core/env.gs
const ENV = {
  VERSION: "v1.0.0",
  ROOT_FOLDER_NAME: "[THAI MAU] DIGITAL OPS",
  // Sau khi chạy RUN_FULL_SYSTEM_INIT(), anh copy ID các sheet dán vào đây
  SPREADSHEET_ID: SpreadsheetApp.getActiveSpreadsheet().getId()
};